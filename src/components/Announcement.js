import React from "react";
import "./compents.css";

// Can any announcement here, this couls be in the data structure at a later date.
//  announcement ? "display" : null
const Announcement = () => {
  return <div className="announcement">Free Shipping On Orders Over $50!</div>;
};

export default Announcement;
